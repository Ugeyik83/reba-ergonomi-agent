streamlit>=1.30.0,<2.0.0
# numpy<2.0.0 kısıtı mediapipe uyumluluğu için zorunlu — mediapipe numpy 2.x'i destekleyene kadar kaldırma
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0
# opencv-python-headless kaldırıldı — contrib paketi tüm işlevselliği kapsar
opencv-contrib-python-headless==4.10.0.84
# mediapipe==0.10.21 sabit — 0.10.22+ sürümlerinde mp.solutions API kaldırıldı
mediapipe==0.10.21
reportlab>=4.0.0
pandas>=2.0.0
pytest>=7.0.0
pytest-cov>=4.0.0
